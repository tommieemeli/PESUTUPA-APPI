import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
      primary: {
        main: "#99EEFF"
      },
      secondary: {
        main: "#13163F"
      },
      tertiary: {
        main: "#6666FF"
      },
      pink: {
        main: "#DCA8DC"
      }
    },
  });

export default theme;