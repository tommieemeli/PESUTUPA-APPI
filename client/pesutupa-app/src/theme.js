import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
      primary: {
        main: "#99EEFF"
      },
      secondary: {
        main: "#13163F"
      },
      pink: {
        main: "#DCA8DC"
      }
    },
  });

export default theme;