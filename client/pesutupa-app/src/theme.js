import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
      primary: {
        main: "#99eeff"
      },
      secondary: {
        main: "#13163f"
      },
      pink: {
        main: "rgb(220, 168, 220)"
      }
    },
  });

export default theme;